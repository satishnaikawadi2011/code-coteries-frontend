mutation addComment($addCommentInput: AddCommentInput!) {
	addComment(addCommentInput: $addCommentInput) {
		eventComment {
			id
			content
			handle
		}
		postComment {
			id
			content
			handle
		}
	}
}
